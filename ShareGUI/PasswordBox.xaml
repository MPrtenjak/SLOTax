<Window x:Class="MNet.SLOTaxGuiTest.PasswordBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MNet.SLOTaxGuiTest"
        mc:Ignorable="d"
        Title="Geslo / Password" SizeToContent="WidthAndHeight">
  <Border BorderBrush="{x:Null}" Padding="20" Background="#FFC8DBEC">
    <StackPanel>
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="30" />
          <RowDefinition Height="20" />
          <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal">
          <TextBlock Text="Vpišite geslo / Enter password :" Margin="0,10,20,0" />
          <PasswordBox x:Name="passwordBox" Width="130" />
        </StackPanel>
        
        <Button Grid.Row="2" Grid.Column="0" Content="OK / OK" Click="btnOK" Margin="0,0,10,0"/>
        <Button Grid.Row="3" Grid.Column="1" Content="Prekini / Cancel" Click="btnCancel" Margin="10,0, 0, 0"/>
      </Grid>
    </StackPanel>
  </Border>
</Window>
